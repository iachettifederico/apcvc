= actions do
  = link_to 'Mostrar', video_path(@video)
  = link_to 'Volver a Tramas', pages_tramas_path

%h1 Editing video

= render 'form'

= actions do
  = link_to 'Mostrar', video_path(@video)
  = link_to 'Volver a Tramas', pages_tramas_path
