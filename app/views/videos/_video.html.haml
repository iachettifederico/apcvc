%hr/
= actions do
  = link_to "Editar", edit_video_path(video)

- present(video) do |video|
  %h2= video.title
  .tramas-article
    %center
      %iframe.video{allowfullscreen: "", frameborder: "0", height: "315", src: video, width: "420"}
