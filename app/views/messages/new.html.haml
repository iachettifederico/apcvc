TEST 2
%h1 Contacto
= simple_form_for message do |f|
  - if message.errors.any?
    .error_messages
      %h2
        = pluralize(message.errors.count, "error")
        prohibited this message from being saved:
      %ul
        - message.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.input :name,    label: "Nombre"
  .field
    = f.input :subject, label: "Asunto"
  .field
    = f.input :email,   label: "e-mail",  as: :email
  .field
    = f.input :content, label: "Mensaje", as: :text
  .actions
    = f.submit "Enviar Mensaje"
