%hr/
= actions do
  = link_to "Editar", edit_announcement_path(announcement)
  = link_to "Eliminar", announcement_path(announcement), confirm: "Esté seguro que desea eliminar este anuncio?", method: :delete
.announcement
  %h2= announcement.title
  .event_date
    Publicado: #{announcement.from_date.strftime("%d/%m/%Y")}
  .announcement_body
    = announcement.content.html_safe
